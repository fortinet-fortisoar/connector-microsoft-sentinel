#### The following enhancements have been made in version 2.0.0 of the Microsoft Sentinel Connector:

- Introduced a new Get Access Token configuration parameter to support the following authentication methods: 
    - Application Permission (Without a User)
    - Delegated Permission (On Behalf of a User)
    - Certificate Based Authentication
- Added support for `Application Permission (Without a User)` and `Certificate-Based Authentication`.